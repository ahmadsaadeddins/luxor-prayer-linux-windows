# Prayer Luxor Desktop

تطبيق سطح مكتب (لينكس وويندوز) يعرض العدّ التنازلي للصلاة التالية في نافذة صغيرة دائمة الظهور — **مواقيت الأقصر فقط**.

## Language

**Prayer times (مواقيت الصلاة)**:
جدول ثابت لمواقيت **الأقصر** — 366 يوماً مضمّناً في التطبيق. **لا** اختيار مدينة ولا اتصال بالإنترنت لحساب المواقيت.
_Avoid_: city، location، API مواقيت

**Widget (الويدجت)**:
نافذة سطح مكتب مستقلة — صغيرة، بدون إطار، دائمة الظهور — تعرض العدّ التنازلي للصلاة التالية على الشاشة.
_Avoid_: extension، plasmoid، تطبيق كامل (للإصدار الأول)

**Widget face (واجهة الويدجت)**:
ما يُعرض دائماً على النافذة الصغيرة — اسم الصلاة التالية بالعربية (مثل «المتبقى على الظهر») والعدّ التنازلي.
_Avoid_: قائمة المواقيت، التاريخ الهجري (في الواجهة الدائمة)

**Prayer times list (قائمة المواقيت)**:
جدول مواقيت اليوم الكامل (فجر، شروق، ظهر، …) — **لا يظهر** في الواجهة الدائمة؛ يُفتح **بالنقر** على الويدجت. **نفس النافذة** تتوسّع في مكانها؛ النقر مرة أخرى أو Escape يُعيد الواجهة المضغوطة.
_Avoid_: شاشة رئيسية، نافذة منفصلة، modal منفصل

**Iqama period (فترة الإقامة)**:
الفترة بين وقت الأذان ونهاية وقت الإقامة — خلالها تنتقل الواجهة الدائمة من العدّ للصلاة التالية إلى العدّ للإقامة. يوم الجمعة: **لا إقامة للظهر** (تُتخطى).
_Avoid_: اقتراب الإقامة، تذكير الإقامة

**Iqama offset (فرق الإقامة)**:
عدد الدقائق **بعد الأذان** حتى الإقامة — **لكل صلاة على حدة**. القيم الافتراضية: فجر 20، شروق 20، ظهر 15، عصر 15، مغرب 5، عشاء 15. يُعدَّل من **قائمة المواقيت** ويُحفظ محلياً.
_Avoid_: iqama minutes، مدة إقامة موحّدة

**Summer time (التوقيت الصيفي)**:
تعديل +60 دقيقة على مواقيت الأقصر — **مفعّل افتراضياً**. مفتاح التفعيل/التعطيل داخل **قائمة المواقيت** (عند النقر)، وليس في الواجهة الدائمة.
_Avoid_: DST، daylight saving (في المحادثة مع المستخدم)

**System tray (أيقونة السيستم)**:
أيقونة في شريط النظام (جنوب Ubuntu / Windows) بينما التطبيق يعمل — **ليس** في شريط المهام. منها: إغلاق التطبيق، تفعيل/تعطيل التشغيل التلقائي.
_Avoid_: taskbar icon، notification icon (وحده)

**Autostart (التشغيل التلقائي)**:
بدء الويدجت عند تسجيل الدخول — **اختياري**، **معطّل افتراضياً**؛ يُفعَّل من قائمة أيقونة السيستم.
_Avoid_: startup on install، launch at boot (بدون تمييز الاختيارية)

**Numerals (عرض الأرقام)**:
شكل الأرقام في العدّ التنازلي ومواقيت القائمة — **هندي عربي** (٠–٩) أو **غربي** (0–9). مفتاح التبديل داخل **قائمة المواقيت**؛ **الافتراضي: هندي عربي**.
_Avoid_: Arabic numerals، locale، unicode digits (في المحادثة)

**Close widget (إغلاق الويدجت)**:
إغلاق **النافذة** (× أو Escape من الواجهة المضغوطة) **لا يُنهي** التطبيق — يُخفى الويدجت إلى أيقونة السيستم ويستمر العدّ. **إنهاء التطبيق** من قائمة أيقونة السيستم فقط.
_Avoid_: quit on close، minimize to tray (في المحادثة — استخدم «إخفاء»)

**Widget position (موقع الويدجت)**:
مكان النافذة على الشاشة — **يُحفظ** بعد السحب و**يُستعاد** عند التشغيل التالي. **الوضع المضغوط** دائماً عند البدء (لا يفتح موسّعاً).
_Avoid_: window coordinates، remember size (في المحادثة)

**Widget theme (مظهر الويدجت)**:
الواجهة **داكنة فقط** في v1 — خلفية داكنة ونص فاتح. **لا** تبديل فاتح/داكن.
_Avoid_: dark mode، light mode، system theme (في المحادثة)

**Single instance (نسخة واحدة)**:
**تشغيل واحد** للتطبيق في كل وقت — إطلاق ثانٍ **لا** ينشئ ويدجتاً آخر؛ يُظهر الويدجت الحالي (أو يُركّز النافذة إن كانت مخفية في السيستم).
_Avoid_: multi-window، duplicate widget
